# Milestone: M6 Wave 2
# Covers: T41 — `get` parse sugar desugars to oget slot-property install.
#
# Each `Foo.value` read triggers the installed getter closure.

var Foo = Object.clone(); Foo.get value() { 42 }; Foo.value
[00000000] 42
