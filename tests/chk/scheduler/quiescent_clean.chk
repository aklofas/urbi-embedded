# Milestone: M3
# Covers: after all strands have run to completion and no pending events,
#         watchers, or sleeping strands remain, sched_quiescent returns true
#         and urbi_step returns URBI_STEP_QUIESCENT.
#
# defer-to: T39
#
# Reason: verifying the QUIESCENT return code from urbi_step requires the
# ## host: mechanism to call urbi_step and inspect its return value from a
# chk fixture.  The REPL-only chk runner at M3 does not support this.
# The quiescence logic itself is fully wired (sched_quiescent checks all 5
# counters; urbi_step returns QUIESCENT when the VM is idle).
# Unit test coverage: tests/unit/test_step_driver.c verifies the QUIESCENT
# path at the C level.
#
# When T39 lands, implement this fixture by:
#   ## host: rc = urbi_step(vm, UINT64_MAX); assert rc == URBI_STEP_QUIESCENT
#   (empty urbiscript — VM starts quiescent)
