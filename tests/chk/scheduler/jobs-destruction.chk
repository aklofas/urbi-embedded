# Milestone: v0.10.10-job-introspection
# Covers: no strand leak after a block containing '&'-joined strands; Job.jobs
#         count returns to baseline after completion.
# blocked: T39 chk driver only
# matrix-row: scheduler-jobs
#
# Reason:
#   Job.jobs() shipped v0.10.10 (D7-B ratify; full-ship variant). The
#   fixture requires the T39 chk driver for `&` inside a block repeated
#   10 times via `for` + multi-strand lifecycle observation. Per-feature
#   coverage in tests/chk/stdlib/runtime/job_jobs_basic.chk.
#
# Original urbiscript (activate when T39 chk driver lands):
# # var njobs = Job.jobs.size|;
# # { for(var i: 10) {1&2&3}, };
# # assert(njobs == Job.jobs.size);
