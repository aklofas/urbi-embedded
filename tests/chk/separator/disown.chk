# Milestone: v0.10.10-job-introspection
# Covers: detach() vs disown() tag-inheritance semantics; detach() inherits tags,
#         disown() strips all tags; Job.current.tags inspection.
# blocked: T39 chk driver only
# matrix-row: separator-detach
#
# Reason:
#   detach() + disown() + Job.current() shipped v0.10.10 (D7-C + D7-A
#   ratifications). The fixture requires the T39 chk driver for the
#   tag-inheritance distinction across `Tag.new():` block-tagged scope +
#   multi-step `sleep(10ms)` observation. Per-feature coverage in
#   tests/chk/separator/detach_basic.chk + disown_basic.chk.
#
# Original urbiscript (activate when T39 chk driver lands):
# # Tag.new(): { var ntags = Job.current.tags.size | {};
# #   assert(ntags >= 1);
# #   detach(echo(Job.current.tags.size - ntags)); sleep(10ms);
# # [00000001] *** 0
# #   disown(echo(Job.current.tags.size)); sleep(10ms);
# # [00000002] *** 0 };
