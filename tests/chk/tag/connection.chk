# Milestone: v0.10.10-job-introspection
# Covers: connection tag is always present in Job.current.tags; tagged blocks
#         add their tag to the ambient tag list visible via Job.current.tags.
# blocked: T39 chk driver only
# matrix-row: scheduler-jobs
#
# Reason:
#   Job.current() + Job.tags() + Lobby.connectionTag all shipped v0.10.10
#   (D7-A / D7-E ratifications). The fixture relies on `&` separator + the
#   T39 chk driver's strand-tag scope-stack observation across multi-step
#   timelines. Per-feature regression coverage already lives in
#   tests/chk/stdlib/runtime/job_current_basic.chk + connection_tag_basic.chk.
#
# Original urbiscript (activate when T39 chk driver lands):
# # assert(Job.current.tags == [connectionTag]);
# # var t = Tag.new() & assert({t: Job.current.tags} == [connectionTag, t]);
# # ...
